services:
  - type: static
    name: bazaar-ecommerce
    rootDir: frontend
    buildCommand: npm install && CI=false npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
